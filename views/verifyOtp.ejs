<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>InfinityTech - Verify OTP</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body class="bg-gradient-to-br from-[#2845AE] via-[#5D2E8C] to-[#A517BA] min-h-screen">
    <div class="container mx-auto px-4 h-screen flex items-center justify-center">
        <div class="max-w-md w-full space-y-8 bg-white p-10 rounded-xl shadow-2xl">
            <div>
                <h2 class="mt-6 text-center text-3xl font-extrabold text-gray-900">
                    Verify Your Email
                </h2>
                <p class="mt-2 text-center text-sm text-gray-600">
                    We've sent a verification code to your email
                    <% if (typeof email !== 'undefined' && email) { %>
                        <span class="font-medium"><%= email %></span>
                    <% } %>
                </p>
            </div>

            <% if (typeof message !== 'undefined' && message) { %>
                <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative" role="alert">
                    <span class="block sm:inline"><%= message %></span>
                </div>
            <% } %>

            <form class="mt-8 space-y-6" action="/verifyOtp" method="POST">
                <div class="rounded-md shadow-sm -space-y-px">
                    <div class="flex justify-center space-x-2">
                        <input type="text" name="otp" maxlength="6" required
                            class="appearance-none relative block w-48 px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 rounded-md focus:outline-none focus:ring-[#2845AE] focus:border-[#2845AE] focus:z-10 sm:text-lg text-center tracking-widest"
                            placeholder="Enter OTP">
                    </div>
                </div>

                <div>
                    <button type="submit"
                        class="group relative w-full flex justify-center py-2 px-4 border border-transparent text-sm font-medium rounded-md text-white bg-[#2845AE] hover:bg-[#1e3690] focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-[#2845AE]">
                        Verify OTP
                    </button>
                </div>
            </form>

            <div class="text-center">
                <p class="text-sm text-gray-600">
                    Didn't receive the code? 
                    <form action="/resendOtp" method="POST" class="inline">
                        <button type="submit" class="font-medium text-[#2845AE] hover:text-[#1e3690]">
                            Resend OTP
                        </button>
                    </form>
                </p>
            </div>

            <div class="mt-4 text-center">
                <p class="text-sm text-gray-500">
                    Please check your email inbox and spam folder. The OTP will expire in 10 minutes.
                </p>
            </div>
        </div>
    </div>

    <!-- Return to Home -->
    <div class="fixed top-4 left-4">
        <a href="/" class="flex items-center text-white hover:text-gray-200">
            <i class="fas fa-arrow-left mr-2"></i>
            Back to Home
        </a>
    </div>

    <script>
        // Auto-submit when all digits are entered
        const otpInput = document.querySelector('input[name="otp"]');
        otpInput.addEventListener('input', function(e) {
            if (this.value.length === 6) {
                this.form.submit();
            }
        });

        // Only allow numbers
        otpInput.addEventListener('keypress', function(e) {
            if (e.key < '0' || e.key > '9') {
                e.preventDefault();
            }
        });
    </script>
</body>
</html>
